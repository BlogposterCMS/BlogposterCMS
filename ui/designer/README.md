# Designer

Target home for the visual builder: canvas, layout tree, selection, history,
toolbar state, widget placement, and publish flows.

The designer should depend on shared contracts instead of server internals.
