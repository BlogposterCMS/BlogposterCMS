# Non-secret Compose interpolation values. Keep CMS credentials exclusively in
# the protected runtime environment referenced by BLOGPOSTER_ENV_FILE.
BLOGPOSTER_ENV_FILE=/opt/blogposter/runtime.env
BLOGPOSTER_OVERRIDES_DIR=/opt/blogposter/customizations/module-overrides
BLOGPOSTER_PROXY_NETWORK=blogposter-proxy
