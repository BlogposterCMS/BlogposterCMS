// Independent of the renderer/token startup graph so a failed boot becomes a
// bounded error with a working reload link. The skeleton itself is already HTML.
import { initializeAdminShellLoading } from '../../shared/feedback/adminShellLoading.js';

initializeAdminShellLoading();
